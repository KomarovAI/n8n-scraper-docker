apiVersion: v1
kind: Service
metadata:
  name: n8n-scraper-external
  namespace: n8n-scraper
spec:
  type: ClusterIP
  selector:
    app: n8n-scraper
  ports:
    - port: 5678
      targetPort: 5678
      protocol: TCP
      name: web
