# RATE_LIMITING_GUIDE.md

## 🎯 Как избежать блокировок и банов при скрапинге

> **tl;dr**: Никогда не запускайте 50+ одновременных табов на 1 сайт! Используйте batch splitting + задержки (delays) между батчами и запросами, рандомизацию и human-like pattern.

---

### Почему важно? 
- Резкий поток (burst) из 50-100 запросов = почти 100% детект Cloudflare/Antibot/DDOS
- Банятся даже доверенные боты: Googlebot MAX 10-20 одновременных запросов
- Современные сайты анализируют time/correlation, user-agent, request patterns (see Akamai, Datadome, Cloudflare)

---

## 🔧 Рецепт: Soft Batched Scraping

1. **Разделяй на небольшие батчи:**
    - Одновременно не больше 5 вкладок/tab (MAX_CONCURRENT_TABS)
2. **Делай паузу между батчами:**
    - Минимум 2-3 секунды (DELAY_BETWEEN_BATCHES)
3. **Рандомизируй задержку:**
    - Для каждого батча +/– 30% задержки (RANDOMIZE_DELAYS)
4. **Задержка между запросами в батче:**
    - 0.3–0.8 секунды (обычно 0.5 сек, DELAY_BETWEEN_REQUESTS)
5. **Имитация человека:**
    - Используй разные User-Agent, user cookies, waitFor actions, случайные mouse moves
6. **Не повторяй одинаковые запросы часто:**
    - Для одинаковых URL: минимум 10 секунд между попытками

### 🔗 Как это реализовано?

```js
const MAX_CONCURRENT_TABS = 5;
const DELAY_BETWEEN_BATCHES = 3000;
const DELAY_BETWEEN_REQUESTS = 500;
const RANDOMIZE_DELAYS = true;
```

Смотри ноду: `workflows/code-nodes/smart-batch-splitting.js`

---

## 🔥 Продвинутые анти-бан техники
- **Прокси rotation** (10+ резидентных прокси)
- **Случайные сценарии поведения:** скроллы, задержки, action chains
- **Adaptive pipeline:** Если детект/блок — увеличь DELAY, снизь MAX_CONCURRENT_TABS
---

## ✅ Пример настройки для production:

| Настройка | Значение |
|-----------|----------|
| MAX_CONCURRENT_TABS    | 5        |
| DELAY_BETWEEN_BATCHES  | 3000 ms  |
| DELAY_BETWEEN_REQUESTS | 500 ms   |
| RANDOMIZE_DELAYS       | true     |


```
# .env.example
MAX_CONCURRENT_TABS=5
DELAY_BETWEEN_BATCHES=3000
DELAY_BETWEEN_REQUESTS=500
RANDOMIZE_DELAYS=true
```

---

## 📋 Итог: Спаси свои домены и свой GitHub :)