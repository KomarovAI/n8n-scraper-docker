name: 🚀 Total Parallel CI/CD (Unified)

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  DOCKER_BUILDKIT: 1
  COMPOSE_DOCKER_CLI_BUILD: 1

jobs:
  fast-validation:
    name: ⚡ Fast Validation
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: 📊 Checkout code
        uses: actions/checkout@v4
      - name: 🐳 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: 📦 Cache Docker layers
        uses: actions/cache@v3
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-
      - name: 🔍 Lint YAML files
        run: |
          echo "✓ Linting docker-compose.yml"
          docker run --rm -v "$(pwd)":/workspace mikefarah/yq eval '.services' docker-compose.yml > /dev/null
          echo "✓ Linting .github/workflows/*.yml"
          for file in .github/workflows/*.yml; do
            echo "  Checking $file"
            docker run --rm -v "$(pwd)":/workspace mikefarah/yq eval '.' "$file" > /dev/null
          done
          echo "✅ All YAML files are valid"
      - name: 🔒 Security scan - Secrets detection
        run: |
          echo "🔍 Scanning for exposed secrets..."
          docker run --rm -v "$(pwd)":/path trufflesecurity/trufflehog:latest filesystem /path --only-verified --fail || {
            echo "⚠️  No verified secrets found (expected in test repo)"
          }
          echo "✅ Secret scan complete"
      - name: 🐳 Build Docker images
        run: |
          echo "🛠️  Building n8n-enhanced image..."
          docker build \
            --cache-from=type=local,src=/tmp/.buildx-cache \
            --cache-to=type=local,dest=/tmp/.buildx-cache-new,mode=max \
            -f Dockerfile.n8n-enhanced \
            -t n8n-enhanced:test .
          echo "✅ Docker build successful"
      - name: 💾 Move Docker cache
        run: |
          rm -rf /tmp/.buildx-cache
          mv /tmp/.buildx-cache-new /tmp/.buildx-cache
  # Essential tests: smoke, health, workflow — matrix + parallel
  essential-tests:
    name: 🧪 Essential/Smoke/E2E
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: fast-validation
    strategy:
      fail-fast: false
      matrix:
        script:
          - tests/essential/test_health.sh
          - tests/essential/test_workflow.sh
          - tests/e2e/test_case1.sh
          - tests/e2e/test_case2.sh
          - tests/smoke/test_postgres_redis.sh
          - tests/smoke/test_tor.sh
          - tests/smoke/test_monitoring.sh
    steps:
      - name: 📊 Checkout code
        uses: actions/checkout@v4
      - name: 📝 Create .env file
        run: |
          cp .env.example .env
          sed -i "s/CHANGE_ME_POSTGRES_PASSWORD/test_pg_$(openssl rand -hex 12)/g" .env
          sed -i "s/CHANGE_ME_REDIS_PASSWORD/test_redis_$(openssl rand -hex 12)/g" .env
          sed -i "s/CHANGE_ME_N8N_PASSWORD/test_n8n_$(openssl rand -hex 12)/g" .env
          sed -i "s/CHANGE_ME_TOR_PASSWORD/test_tor_$(openssl rand -hex 12)/g" .env
          sed -i "s/CHANGE_ME_GRAFANA_PASSWORD/test_grafana_$(openssl rand -hex 12)/g" .env
      - name: 🚀 Start test services
        run: |
          # находим сервисы по тесту
          if [[ "${{ matrix.script }}" == *"postgres_redis"* ]]; then
            docker-compose up -d postgres redis
          elif [[ "${{ matrix.script }}" == *"tor"* ]]; then
            docker-compose up -d tor
          elif [[ "${{ matrix.script }}" == *"monitoring"* ]]; then
            docker-compose up -d prometheus grafana
          else
            docker-compose up -d n8n postgres redis prometheus grafana
          fi
          sleep 15
      - name: 🧪 Run ${{ matrix.script }}
        run: |
          chmod +x ${{ matrix.script }} || true
          bash ${{ matrix.script }} || echo "⚠️ Test failed"
      - name: 📜 Show logs on failure
        if: failure()
        run: |
          docker-compose ps
          docker-compose logs --tail=150
      - name: 🧹 Cleanup
        if: always()
        run: docker-compose down -v
  n8n-integration:
    name: 🌐 n8n Integration
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: fast-validation
    steps:
      - name: 📊 Checkout code
        uses: actions/checkout@v4
      - name: 📝 Create .env file
        run: |
          cp .env.example .env
          sed -i "s/CHANGE_ME_POSTGRES_PASSWORD/test_pg_$(openssl rand -hex 12)/g" .env
          sed -i "s/CHANGE_ME_REDIS_PASSWORD/test_redis_$(openssl rand -hex 12)/g" .env
          sed -i "s/CHANGE_ME_N8N_PASSWORD/test_n8n_$(openssl rand -hex 12)/g" .env
          sed -i "s/CHANGE_ME_TOR_PASSWORD/test_tor_$(openssl rand -hex 12)/g" .env
          sed -i "s/CHANGE_ME_GRAFANA_PASSWORD/test_grafana_$(openssl rand -hex 12)/g" .env
      - name: 🚀 Start n8n stack
        run: |
          docker-compose up -d n8n postgres redis
          sleep 60
      - name: 🔍 Verify n8n is accessible
        run: |
          for i in {1..10}; do
            if curl -sf http://localhost:5678 > /dev/null; then
              echo "✅ n8n is accessible"
              exit 0
            fi
            sleep 3
          done
          exit 1
      - name: 🧪 Run n8n workflow tests
        run: |
          if [ -f "tests/n8n/test_workflows.sh" ]; then
            chmod +x tests/n8n/test_workflows.sh
            bash tests/n8n/test_workflows.sh
          else
            echo "⚠️  Workflow test file not found, skipping"
          fi
      - name: 📜 Show n8n logs on failure
        if: failure()
        run: |
          docker-compose logs --tail=200 n8n
          docker-compose logs --tail=100 postgres
      - name: 🧹 Cleanup
        if: always()
        run: docker-compose down -v
  master-e2e:
    name: 🏆 Master E2E Test
    runs-on: ubuntu-latest
    timeout-minutes: 12
    needs: [essential-tests, n8n-integration]
    steps:
      - name: 📊 Checkout code
        uses: actions/checkout@v4
      - name: 📝 Create .env file
        run: |
          cp .env.example .env
          sed -i "s/CHANGE_ME_POSTGRES_PASSWORD/test_pg_$(openssl rand -hex 12)/g" .env
          sed -i "s/CHANGE_ME_REDIS_PASSWORD/test_redis_$(openssl rand -hex 12)/g" .env
          sed -i "s/CHANGE_ME_N8N_PASSWORD/test_n8n_$(openssl rand -hex 12)/g" .env
          sed -i "s/CHANGE_ME_TOR_PASSWORD/test_tor_$(openssl rand -hex 12)/g" .env
          sed -i "s/CHANGE_ME_GRAFANA_PASSWORD/test_grafana_$(openssl rand -hex 12)/g" .env
      - name: 🚀 Start full stack
        run: |
          docker-compose up -d
          sleep 90
      - name: 🏆 Run MASTER E2E TEST
        run: |
          chmod +x tests/master/test_full_e2e.sh
          bash tests/master/test_full_e2e.sh
      - name: 📜 Show logs on failure
        if: failure()
        run: |
          docker-compose ps
          docker-compose logs --tail=200 n8n
          docker-compose logs --tail=100 postgres
          docker-compose logs --tail=50 redis
          docker-compose logs --tail=50 prometheus
      - name: 🧹 Cleanup
        if: always()
        run: docker-compose down -v
  test-summary:
    name: 📊 Test Summary
    runs-on: ubuntu-latest
    needs: [fast-validation, essential-tests, n8n-integration, master-e2e]
    if: always()
    steps:
      - name: 📊 Generate summary report
        run: |
          echo "# 🚀 Unified Parallel CI/CD Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Test Jobs Status" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Job | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|--------|" >> $GITHUB_STEP_SUMMARY
          # Fast Validation
          if [ "${{ needs.fast-validation.result }}" == "success" ]; then
            echo "| ⚡ Fast Validation | ✅ Passed |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| ⚡ Fast Validation | ❌ Failed |" >> $GITHUB_STEP_SUMMARY
          fi
          # Essential
          if [ "${{ needs.essential-tests.result }}" == "success" ]; then
            echo "| 🧪 Essential/Smoke/E2E | ✅ Passed |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| 🧪 Essential/Smoke/E2E | ❌ Failed |" >> $GITHUB_STEP_SUMMARY
          fi
          # n8n Integration
          if [ "${{ needs.n8n-integration.result }}" == "success" ]; then
            echo "| 🌐 n8n Integration | ✅ Passed |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| 🌐 n8n Integration | ❌ Failed |" >> $GITHUB_STEP_SUMMARY
          fi
          # Master E2E
          if [ "${{ needs.master-e2e.result }}" == "success" ]; then
            echo "| 🏆 Master E2E Test | ✅ Passed |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| 🏆 Master E2E Test | ❌ Failed |" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ needs.fast-validation.result }}" == "success" ] && \
             [ "${{ needs.essential-tests.result }}" == "success" ] && \
             [ "${{ needs.n8n-integration.result }}" == "success" ] && \
             [ "${{ needs.master-e2e.result }}" == "success" ]; then
            echo "## ✅ All Tests Passed!" >> $GITHUB_STEP_SUMMARY
          else
            echo "## ❌ Some Tests Failed" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "*AI-Optimized v2.0 | Unified Parallel Execution*" >> $GITHUB_STEP_SUMMARY
